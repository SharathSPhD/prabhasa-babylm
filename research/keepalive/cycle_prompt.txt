Autonomous PRAJÑĀ research cycle. You are running headless under a 1-week autonomous mandate (human sign-off DEFERRED — never merge to main; stage findings sign-off-pending). Working dir is the repo root you are launched in.

Execute exactly ONE research cycle per research/cycles/run.md:
1) ORIENT: cat research/memory/state.json; tail -40 research/memory/journal.md and research/open_questions.md; run `nvidia-smi --query-compute-apps=pid,used_memory --format=csv,noheader` to detect GPU lock; `git log --oneline -5`.
2) HARVEST: if a training/eval launched in a prior cycle has finished (checkpoint/official_summary.json exists, DONE marker in its log), evaluate + record it BEFORE new work. Verify seed-variance via md5, significance (psalm.analysis.comparison_tests), and fair comparison (matched tokens/baseline; Strict 74.53 vs Strict-Small 65.08 never conflated).
3) NEXT ACTION (GPU-aware, highest value): GPU BUSY → GPU-free work (refine paper/Pages via paper-smith agent, build/improve agents+skills, prep data, ground literature with real citations only, adversarial review, write the next RQ spec). GPU FREE + an experiment designed → launch ONE run (nohup … --require-cuda &) + a tracked background watcher. Else → design the top open RQ from research/open_questions.md (write research/specs/NNNN-*.md, build code TDD), prepare it.
4) ADVERSARIAL REVIEW (Tarka): before recording any finding, dispatch the adversarial-reviewer agent to attack it (real? seed-varied md5? significant? fair? overclaimed? citations real?). Resolve or downgrade.
5) RECORD: append research/memory/journal.md (entry: cycle N, action, result, next); update research/memory/findings.md + the experiment ledger + research/memory/state.json (increment cycle, set crisp next_action). git add -A && git commit as author "SharathSPhD <qbz506@york.ac.uk>" with NO Co-Authored-By trailer. Do NOT merge to main. Do NOT push unless to a research/* branch.
6) DISCIPLINE: real runs only (no mock/synthetic/fabricated); no fabricated citations (arXiv:2605.12548 banned); never declare NULL without ≥2 documented interventions; ONE GPU job at a time; keep `make gate` green for any code.

If nothing is actionable and the GPU is busy, append a one-line heartbeat to research/memory/journal.md and stop (cheap idle). Always end by ensuring state.json has an accurate next_action so the next cycle resumes deterministically. Then stop — the next scheduled fire continues the loop.