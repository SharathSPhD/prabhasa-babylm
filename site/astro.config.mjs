import { defineConfig } from "astro/config";

// GitHub Pages project site for SharathSPhD/PSALM.
// Served at https://SharathSPhD.github.io/PSALM/ — hence the base path.
export default defineConfig({
  site: "https://SharathSPhD.github.io",
  base: "/PSALM",
});
