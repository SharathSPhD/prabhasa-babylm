% ---- introduction.tex ----
% Data sources: /home/sharaths/projects/PSALM-integration/research/memory/findings.md
% /home/sharaths/projects/PSALM-integration/site/src/data/results.json
% /home/sharaths/projects/PSALM-integration/.claude/worktrees/practical-mendel-bf73a8/paper/references_new.bib

\section{Introduction}

The scaling of language model pretraining has driven rapid progress in natural language understanding and downstream task performance. Yet the relationship between data efficiency, inductive bias, and model behaviour remains underspecified, particularly at smaller scales. Small language models---operating under strict computational budgets (10M to 150M parameters)---offer a controlled experimental testbed for understanding which architectural and training choices yield measurable improvements, free from the confounds that emerge when training frontier-scale models. Understanding what matters at small scales is therefore both scientifically and practically important: it constrains the design space for data-efficient pretraining and clarifies which inductive biases translate across scales.

One broad class of inductive biases derives from linguistic theory. Classical Pańinian grammar~\cite{Matilal2001}, developed over more than a millennium, encodes compositional structure at the morpheme level (through morphological segmentation and inflectional rules) and at the argument-level (through systematic kāraka categories that denote semantic roles). These categories and their combinatorial rules are known to capture syntactic and semantic patterns in natural language. Whether embedding such structure as explicit inductive biases in the pretraining procedure improves data efficiency for small language models remains an open empirical question. The motivation is intuitive: if a linguistically grounded prior reduces the effective hypothesis space the model must search, it should require fewer training tokens to achieve a given level of generalisation.

This paper investigates this hypothesis through a controlled experimental study of Pańinian-motivated mechanisms within the BabyLM constrained-data setting~\cite{Warstadt2023}. The program, called Pāṇinian Structured pretraining for Small LAnguage Models (PSALM), comprises three research hypotheses:

\emph{(H1, primary).} Does morpheme-boundary structure (implemented as Vidyut N-hot embeddings~\cite{Vidyut}) combined with role-aware masking (kāraka-stratified masking probability) measurably improve compositional generalisation on English syntax benchmarks (BLiMP agreement and argument-structure subsets) under matched training budgets and controlled comparisons?

H1 decomposes into two mechanistically distinct sub-questions that isolate different potential levers:

\emph{(RQ-A, masking causality).} At matched mask-rate budget, does kāraka role-stratification of token masking carry causal efficacy on the targeted BLiMP paradigms? This question asks whether altering which tokens are masked---based on their morphological or semantic-role structure---affects downstream syntax performance.

\emph{(RQ-B, auxiliary objective).} Does a supervised auxiliary objective (token-level kāraka-role prediction from the contextualized embeddings of a pure-MLM model) improve BLiMP performance in cases where the masking lever (RQ-A) was ineffective? This question asks whether explicit role supervision provides a signal the masking strategy alone does not.

\emph{(H2, synergy in scope).} On the best H1-validated arm, does a 6-phase Pañcāvayava fine-tuning scaffold reduce fallacious-inference rates and improve sample efficiency on reasoning-quality readouts versus a matched generic baseline?

\emph{(H3, epistemic constraint, out of scope).} Can a GBNF schema coupled with a Z3 vyāpti verifier enforce causal-validity constraints by construction?

The primary evaluation focuses on H1 with pre-registered thresholds (e.g., ≥+1.0 percentage point gain on targeted paradigm subsets; significance at ≥3 independent seeds with paired bootstrap resampling and Holm--Bonferroni family correction). Pre-registration serves a critical role: it prevents post-hoc rationalisation of weak or null findings and enforces statistical discipline before results are known.

The contributions of this work are as follows.

\emph{(C1) Controlled mechanism ablation.} RQ-A and RQ-B are tested independently using budget-matched arms to isolate the causal role of masking distribution from that of auxiliary supervision. This separation rules out confounds (such as differences in effective mask rate) that have plagued earlier single-arm comparisons and prevented clear mechanistic conclusions.

\emph{(C2) Scale-dependent objective effects.} An orthogonal finding during the ablation revealed that the choice between pure masked-language modelling (MLM) and hybrid MLM+causal-language-modelling (CLM) objectives is scale-dependent. At 10M tokens, the two objectives are statistically equivalent (pure-MLM 63.58\,\%±1.73 vs hybrid 64.09\,\%±0.26 on BLiMP, overlapping confidence intervals). At 100M tokens, pure-MLM substantially outperforms hybrid MLM+CLM (73.06\,\% vs 67.57\,\%, a 5.49 percentage-point gain). This finding was validated independently and suggests that the CLM head's dilution of MLM training quality is a scale-dependent bottleneck in small-model pretraining.

\emph{(C3) Statistical rigour and transparency.} All hypothesis tests use ≥3 independent random seeds, paired bootstrap resampling within pre-registered target sets, and explicit pre-registration of outcome thresholds and inference procedures. A separate Tarka memo (the strongest objection to one's own finding) is prepared for each result to identify confounds and alternative explanations before closure.

\emph{(C4) Reproducible release.} Code, data, model checkpoints, and per-seed results are released openly (HuggingFace model cards, GitHub repository) with full hyperparameter disclosure and configuration hashing, enabling replication and downstream use by the community.

To the best of the authors' knowledge, this is the first work to apply pre-registered, multi-seed, mechanistically-decomposed testing of Pańinian linguistic structure as an inductive bias for masked-language-model pretraining under strict data budgets. Prior work on linguistically motivated pretraining (for instance, morpheme-aware objectives~\cite{Yaltik2020} and morphologically grounded word representations~\cite{Botha2014Morphology}) has tested linguistic priors in isolation or on larger models. PSALM combines independent ablation of two mechanistically distinct levers (masking vs auxiliary objective), multi-seed validation, pre-registered significance thresholds, and explicit control for confounds. The distinctive contribution is therefore the mechanistic decomposition and statistical discipline, rather than any individual technique; together, these elements constitute a novel experimental design for isolating the causal efficacy of linguistically motivated structure in small-model pretraining.

The empirical findings from H1 indicate that architectural choices (RoPE positional encoding~\cite{Su2021RoPE}) and objective design (pure MLM without auxiliary CLM) are the primary levers for improving compositional generalisation at this scale (10M to 100M tokens). The Pańinian mechanisms---masking stratification and auxiliary kāraka-role prediction---show null or marginal effects when tested with appropriate controls. Specifically, RQ-A (masking causality) yields a pre-registered null finding at 100M (Arm K vs Arm C: ΔK−C=+0.10 percentage points, 95\,\% CI [−0.99, +1.20], non-significant). RQ-B (auxiliary objective) exhibits a weak, non-significant effect at 10M when tested across 5 seeds (ΔAux−Base=+0.76 percentage points, 95\,\% CI [−0.74, +2.27], t=1.41, df=4, not significant), with the single-seed ''positive'' result shrinking after adding more seeds, indicating seed variance rather than robust signal.

This outcome is scientifically valuable: it constrains the scope of claims about structure-driven pretraining, identifies the true levers (architecture and objective), and provides a rigorous null-finding baseline for the community. A well-documented null is a valid and necessary contribution, especially when it overturns optimistic prior expectations and is grounded in pre-registered, multi-seed validation.

The remainder of this paper is structured as follows. Section~\ref{sec:related} reviews related work on inductive biases in pretraining, data-efficient language model training, and computational treatments of Pāṇinian grammar and Navya-Nyāya. Section~\ref{sec:data} describes the corpora, the synthetic Pāṇinian pre-pretraining dose, and the four dose arms. Section~\ref{sec:methods} introduces the model architecture and the Pāṇinian mechanisms, and states the research questions and their pre-registered thresholds. Section~\ref{sec:training} describes the training stages, the fine-tuning procedure, and the evaluation protocol. Section~\ref{sec:results} reports results for each research question with effect estimates and confidence intervals. Section~\ref{sec:discussion} discusses the interpretation of the findings, their implications for small-model pretraining, and threats to validity. Section~\ref{sec:conclusion} concludes and outlines future work. Supplementary configuration details, per-seed tables, and reproducibility information are provided in Appendix~\ref{app:repro}.
