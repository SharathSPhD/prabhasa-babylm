# Serena project configuration for PSALM.
project_name: psalm
language: python
# Directories Serena should ignore when indexing.
ignored_paths:
  - .venv
  - .git
  - data/synthetic
  - data/corpora
  - data/training
  - site/node_modules
  - outputs
  - checkpoints
read_only: false
